```html
<apex:page >
    <script>
    // 형 변환
    
    // Truthy(참 같은 값)
    // true, {}, [], 1, 2, 'false', -12, '3.14' ...
    
    // Falsy(거짓 같은 값)
    // false, '', null, undefined, 0, -0, NaN(Not a Number)
    
    if('false') {
    	console.log(123);
    }else {
    	console.log('false');
    }
    </script>
</apex:page>

```
